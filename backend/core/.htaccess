# Block all access to internal directories if Apache is used and DocumentRoot is not set to /public
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
